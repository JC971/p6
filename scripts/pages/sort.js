const like=[]

for (let i = 0; i < Media.length; i++){
if(like[i])
}
function compare(a, b) {
    if (a < b)
        return -1;
    if (a > b)
        return 1;
    return 0;
}

let tabTrie = tab.sort(compare)
console.log(tabTrie)